# 📧 คำแนะนำการตั้งค่า Firebase สำหรับการรีเซ็ตรหัสผ่าน

## ✅ สิ่งที่แก้ไขไปแล้ว

1. ✅ เพิ่ม Firebase API Key ใน .env
2. ✅ ปรับปรุงฟังก์ชัน resetPassword() ให้ตรวจสอบอีเมลก่อนส่ง
3. ✅ เพิ่มการแสดง error messages เป็นภาษาไทย
4. ✅ เพิ่ม console logs สำหรับ debug
5. ✅ Deploy ไปที่ production แล้ว

## ⚙️ สิ่งที่ต้องตรวจสอบใน Firebase Console

### 🔴 สำคัญมาก - ต้องทำก่อนถึงจะส่งอีเมลได้!

#### 1. เปิดใช้งาน Email Template
```
1. เปิด https://console.firebase.google.com/project/peace-script-ai/authentication/emails
2. คลิกที่ "Password reset" (หรือ "รีเซ็ตรหัสผ่าน")
3. ตรวจสอบว่า:
   - ✅ Email template เปิดใช้งานอยู่
   - ✅ Sender name: ตั้งเป็น "Peace Script AI"
   - ✅ Sender email: noreply@peace-script-ai.firebaseapp.com
   - ✅ กด Save ด้วย
```

#### 2. ตั้งค่า Authorized Domains
```
1. เปิด https://console.firebase.google.com/project/peace-script-ai/authentication/settings
2. ไปที่แท็บ "Authorized domains"
3. ตรวจสอบว่ามี:
   - ✅ peace-script-ai.web.app
   - ✅ peace-script-ai.firebaseapp.com
   - ✅ localhost (สำหรับทดสอบ)
```

#### 3. ตรวจสอบ Email/Password เปิดใช้งาน
```
1. เปิด https://console.firebase.google.com/project/peace-script-ai/authentication/providers
2. ตรวจสอบว่า "Email/Password" เปิดใช้งาน (Enabled)
```

## 🧪 วิธีทดสอบ

### ทดสอบบน Production:
```
1. เปิด https://peace-script-ai.web.app
2. กด F12 เปิด DevTools (เพื่อดู Console)
3. คลิก "ลืมรหัสผ่าน?"
4. กรอกอีเมลที่ลงทะเบียนไว้แล้ว
5. คลิก "ส่งลิงก์รีเซ็ตรหัสผ่าน"
6. ดูที่ Console ว่ามี log อะไร
```

### สิ่งที่ควรเห็นใน Console:
```
✅ ถ้าสำเร็จ:
   "Attempting to send password reset email..."
   "Password reset email sent successfully!"

❌ ถ้าผิดพลาด:
   "Password reset error: ..."
   "Error code: auth/..."
```

### ตรวจสอบอีเมล:
```
1. เช็คที่ inbox ของอีเมลที่กรอก
2. เช็คที่ Spam/Junk folder ด้วย
3. รออีเมลประมาณ 1-2 นาที
4. อีเมลจะมาจาก: noreply@peace-script-ai.firebaseapp.com
```

## 🐛 การแก้ไขปัญหา

### ปัญหา: ไม่ส่งอีเมล
```
✅ ตรวจสอบ:
   1. Firebase Console > Authentication > Templates > Password reset เปิดใช้งานหรือยัง
   2. Firebase Console > Authentication > Users มี user อีเมลนี้หรือไม่
   3. ดู Console logs ว่ามี error code อะไร
   4. ลองเข้า Spam folder
```

### Error Codes ที่พบบ่อย:

| Error Code | ความหมาย | วิธีแก้ |
|-----------|----------|---------|
| `auth/user-not-found` | ไม่พบอีเมลในระบบ | ให้ user สมัครสมาชิกก่อน |
| `auth/invalid-email` | อีเมลผิดรูปแบบ | ตรวจสอบว่ากรอกอีเมลถูกต้อง |
| `auth/too-many-requests` | ส่งคำขอบ่อยเกินไป | รอ 5-10 นาทีแล้วลองใหม่ |
| `auth/configuration-not-found` | ไม่ได้ตั้งค่า template | ไป Firebase Console ตั้งค่า Password reset template |
| `auth/invalid-continue-uri` | URL ไม่ได้อยู่ใน Authorized domains | เพิ่ม domain ใน Authorized domains |

## 📝 Checklist

ก่อนทดสอบ ให้ทำตาม checklist นี้:

- [ ] เปิด Firebase Console > Authentication > Templates > Password reset
- [ ] ตั้งค่า Sender name และ email
- [ ] บันทึกการตั้งค่า (Save)
- [ ] ตรวจสอบ Authorized domains มี production URL
- [ ] ตรวจสอบว่ามี user ในระบบ (หรือสมัครใหม่)
- [ ] ลองกดปุ่ม "ลืมรหัสผ่าน?"
- [ ] กรอกอีเมลที่ลงทะเบียนไว้
- [ ] ดู Console logs
- [ ] ตรวจสอบอีเมล inbox และ spam

## 🎯 ถ้าทำครบทุกขั้นตอนแล้ว

อีเมลจะถูกส่งไปที่อีเมลที่กรอก ภายใน 1-2 นาที

คลิกลิงก์ในอีเมล → เปลี่ยนรหัสผ่านใหม่ → กลับมา login ได้เลย!

## 📞 ถ้ายังมีปัญหา

ส่ง screenshot ของ:
1. Console logs (F12 > Console tab)
2. Network tab (F12 > Network tab, filter: "password")
3. Firebase Console > Authentication > Templates > Password reset

จะได้ช่วย debug ต่อได้ครับ
