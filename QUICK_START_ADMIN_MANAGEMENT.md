# 🚀 Quick Start Guide - Admin Management System

## เริ่มใช้งานภายใน 5 นาที!

### ขั้นตอนที่ 1: สร้าง Super Admin คนแรก

เนื่องจากต้องมี super-admin อย่างน้อย 1 คนเพื่อจัดการ admin คนอื่น ให้ทำดังนี้:

#### วิธีที่ 1: ใช้ Firebase Console (แนะนำ)

1. เข้า Firebase Console: https://console.firebase.google.com
2. เลือกโปรเจค "peace-script-ai"
3. ไปที่ **Authentication** → **Users**
4. คลิกที่ user ที่ต้องการให้เป็น super-admin
5. คัดลอก **User UID**
6. ไปที่ **Firestore Database**
7. สร้าง document ใหม่ใน collection `admin-users`:

```
Collection: admin-users
Document ID: [User UID ที่คัดลอกมา]

Fields:
- email: "youremail@example.com" (string)
- role: "super-admin" (string)
- permissions: (map)
  - canViewAnalytics: true (boolean)
  - canExportData: true (boolean)
  - canManageUsers: true (boolean)
  - canManageSubscriptions: true (boolean)
- createdAt: [คลิก "Use server timestamp"]
- createdBy: "system" (string)
```

8. บันทึก document
9. ไปที่ **Functions** → เลือก function `grantAdminAccess`
10. ไปที่แท็บ **Testing**
11. ใส่ข้อมูลทดสอบ:

```json
{
  "data": {
    "email": "youremail@example.com",
    "role": "super-admin",
    "permissions": {
      "canViewAnalytics": true,
      "canExportData": true,
      "canManageUsers": true,
      "canManageSubscriptions": true
    }
  }
}
```

12. เสร็จแล้ว! ตอนนี้ user นี้มีสิทธิ์ super-admin

#### วิธีที่ 2: ใช้ Firebase CLI (สำหรับ Developer)

สร้างไฟล์ `scripts/create-super-admin.js`:

```javascript
const admin = require('firebase-admin');

// Initialize Firebase Admin
const serviceAccount = require('../path/to/serviceAccountKey.json');
admin.initializeApp({
  credential: admin.credential.cert(serviceAccount)
});

const db = admin.firestore();

async function createSuperAdmin(email) {
  try {
    // Get user by email
    const user = await admin.auth().getUserByEmail(email);
    
    // Set custom claims
    await admin.auth().setCustomUserClaims(user.uid, {
      admin: true,
      adminRole: 'super-admin'
    });
    
    // Save to Firestore
    await db.collection('admin-users').doc(user.uid).set({
      email: email,
      role: 'super-admin',
      permissions: {
        canViewAnalytics: true,
        canExportData: true,
        canManageUsers: true,
        canManageSubscriptions: true
      },
      createdAt: admin.firestore.FieldValue.serverTimestamp(),
      createdBy: 'system'
    });
    
    console.log(`✅ Super admin created: ${email}`);
    console.log(`User ID: ${user.uid}`);
  } catch (error) {
    console.error('❌ Error:', error);
  }
}

// ใช้งาน
createSuperAdmin('youremail@example.com');
```

รัน script:
```bash
node scripts/create-super-admin.js
```

---

### ขั้นตอนที่ 2: เข้าสู่ระบบและทดสอบ

1. เปิดเว็บ: https://peace-script-ai.web.app
2. **Logout** ถ้าเข้าสู่ระบบอยู่แล้ว (เพื่อ refresh custom claims)
3. **Login** ด้วย email ที่ตั้งเป็น super-admin
4. ไปที่ **/admin** หรือคลิกเมนู Admin Dashboard
5. คลิกแท็บ **User Management**
6. ควรเห็นปุ่ม "➕ เพิ่ม Admin" และรายการ admin ตัวเอง

---

### ขั้นตอนที่ 3: เพิ่ม Admin คนอื่น

#### 3.1 เพิ่ม Admin แบบ Viewer (Read-Only)

1. คลิกปุ่ม "➕ เพิ่ม Admin"
2. กรอก email: `viewer@example.com`
3. เลือก Role: **Viewer**
4. Permissions จะ auto-set เป็น:
   - ✅ ดู Analytics
   - ❌ Export ข้อมูล
   - ❌ จัดการ Admin
   - ❌ จัดการ Subscriptions
5. คลิก "✅ เพิ่ม Admin"

#### 3.2 เพิ่ม Admin แบบ Admin (Can Manage Data)

1. คลิกปุ่ม "➕ เพิ่ม Admin"
2. กรอก email: `admin@example.com`
3. เลือก Role: **Admin**
4. Permissions จะ auto-set เป็น:
   - ✅ ดู Analytics
   - ✅ Export ข้อมูล
   - ❌ จัดการ Admin
   - ✅ จัดการ Subscriptions
5. คลิก "✅ เพิ่ม Admin"

#### 3.3 เพิ่ม Super Admin คนอื่น

1. คลิกปุ่ม "➕ เพิ่ม Admin"
2. กรอก email: `superadmin2@example.com`
3. เลือก Role: **Super Admin**
4. Permissions จะ auto-set เป็น:
   - ✅ ดู Analytics
   - ✅ Export ข้อมูล
   - ✅ จัดการ Admin
   - ✅ จัดการ Subscriptions
5. คลิก "✅ เพิ่ม Admin"

---

### ขั้นตอนที่ 4: ทดสอบการแก้ไข

1. หาการ์ดของ admin ที่ต้องการแก้ไข
2. คลิกปุ่ม "✏️ แก้ไข"
3. เปลี่ยน role จาก Viewer → Admin
4. คลิก "💾 บันทึกการเปลี่ยนแปลง"
5. รอสักครู่ list จะ refresh อัตโนมัติ
6. ตรวจสอบว่า role เปลี่ยนแล้ว

---

### ขั้นตอนที่ 5: ทดสอบการลบ

1. หาการ์ดของ admin ที่ต้องการลบ
2. คลิกปุ่ม "🗑️ ลบ"
3. ยืนยันใน dialog
4. รอสักครู่ admin จะหายจาก list

---

## 🎯 Use Cases ตัวอย่าง

### Use Case 1: ทีม Customer Support

**ความต้องการ:** ต้องการให้ทีม Support ดู analytics ได้ แต่ไม่ให้แก้ไขอะไร

**วิธีการ:**
1. เพิ่ม admin ด้วย role = **Viewer**
2. Permissions:
   - ✅ ดู Analytics
   - ❌ Export ข้อมูล
   - ❌ จัดการ Admin
   - ❌ จัดการ Subscriptions

### Use Case 2: ทีม Operations

**ความต้องการ:** จัดการ subscriptions และ export data ได้ แต่ไม่ให้จัดการ admin

**วิธีการ:**
1. เพิ่ม admin ด้วย role = **Admin**
2. Permissions:
   - ✅ ดู Analytics
   - ✅ Export ข้อมูล
   - ❌ จัดการ Admin
   - ✅ จัดการ Subscriptions

### Use Case 3: CTO / Technical Lead

**ความต้องการ:** สิทธิ์เต็มทุกอย่าง รวมถึงจัดการทีม admin

**วิธีการ:**
1. เพิ่ม admin ด้วย role = **Super Admin**
2. Permissions:
   - ✅ ดู Analytics
   - ✅ Export ข้อมูล
   - ✅ จัดการ Admin
   - ✅ จัดการ Subscriptions

---

## ⚠️ ข้อควรระวัง

### 1. ห้ามลบ Super Admin ตัวสุดท้าย
- ถ้าลบ super-admin คนสุดท้าย จะไม่มีใครจัดการ admin ได้
- ควรมี super-admin อย่างน้อย 2 คน

### 2. ไม่สามารถแก้ไข/ลบตัวเอง
- ระบบจะ disable ปุ่มถ้าพยายามแก้ไข/ลบตัวเอง
- ต้องให้ super-admin คนอื่นแก้ไขให้

### 3. Custom Claims ต้อง Refresh
- หลังจากเปลี่ยน role/permissions ต้อง logout แล้ว login ใหม่
- หรือใช้ Incognito mode

### 4. User ต้องมีบัญชีอยู่แล้ว
- เพิ่ม admin ได้เฉพาะ user ที่ register แล้วเท่านั้น
- ถ้ายังไม่มีบัญชี ให้ user สมัครก่อน

---

## 🔍 Verification Checklist

หลังจากเพิ่ม admin แล้ว ตรวจสอบดังนี้:

### ✅ ตรวจสอบใน Frontend
- [ ] เห็นชื่อใน User Management list
- [ ] Role แสดงถูกต้อง
- [ ] Permissions badges แสดงถูกต้อง
- [ ] วันที่สร้างแสดงถูกต้อง

### ✅ ตรวจสอบใน Firebase Console
- [ ] ไปที่ Firestore → admin-users
- [ ] เห็น document ของ admin ที่เพิ่ม
- [ ] ข้อมูลครบถ้วน (email, role, permissions, createdAt, createdBy)

### ✅ ตรวจสอบ Audit Log
- [ ] ไปที่ Firestore → admin-audit-log
- [ ] เห็น log action = "grant-admin-access"
- [ ] มี adminId ของคนที่เพิ่ม
- [ ] มี targetUserId ของคนที่ถูกเพิ่ม
- [ ] มี timestamp

### ✅ ทดสอบ Login
- [ ] Logout จากระบบ
- [ ] Login ด้วย email ของ admin ใหม่
- [ ] เข้า /admin ได้
- [ ] เห็นเฉพาะฟีเจอร์ที่มีสิทธิ์

---

## 🎓 Best Practices

### 1. Principle of Least Privilege
- ให้สิทธิ์เท่าที่จำเป็นเท่านั้น
- เริ่มจาก Viewer ก่อน แล้วค่อยเพิ่มสิทธิ์ตามความจำเป็น

### 2. Regular Audit
- ตรวจสอบ admin list เป็นประจำ
- ลบ admin ที่ไม่ได้ใช้งานแล้ว
- ดู audit log เป็นประจำ

### 3. Multiple Super Admins
- ควรมี super-admin อย่างน้อย 2-3 คน
- กระจายสิทธิ์เพื่อป้องกันปัญหาถ้ามีคนลาออก

### 4. Documentation
- เขียนเอกสารว่า admin แต่ละคนมีสิทธิ์อะไร
- อัพเดทเอกสารเมื่อมีการเปลี่ยนแปลง

---

## 📞 ติดต่อ Support

หากมีปัญหาหรือข้อสงสัย:

1. **ตรวจสอบเอกสาร:**
   - `ADMIN_MANAGEMENT_IMPLEMENTATION.md` - เอกสารทางเทคนิค
   - `DEPLOYMENT_SUCCESS_ADMIN_MANAGEMENT.md` - สรุปการ deploy

2. **ตรวจสอบ Logs:**
   - Firebase Console → Functions → Logs
   - Browser Console (F12)

3. **Common Issues:**
   - ดู Troubleshooting section ใน DEPLOYMENT_SUCCESS_ADMIN_MANAGEMENT.md

---

## ✅ Final Checklist

ก่อนใช้งานจริง ตรวจสอบ:

- [ ] มี super-admin อย่างน้อย 1 คน
- [ ] ทดสอบเพิ่ม admin สำเร็จ
- [ ] ทดสอบแก้ไข admin สำเร็จ
- [ ] ทดสอบลบ admin สำเร็จ
- [ ] ทดสอบ permissions ทำงานถูกต้อง
- [ ] ทดสอบบนมือถือ (responsive)
- [ ] อ่านเอกสารครบถ้วน
- [ ] บันทึกข้อมูล super-admin ไว้ที่ปลอดภัย

---

**ขอให้การใช้งานราบรื่น! 🎉**

**URL:** https://peace-script-ai.web.app/admin  
**Version:** 1.0.0  
**Updated:** 19 ธันวาคม 2568
