# Peace Script AI - Quick Start Guide

**เริ่มต้นใช้งาน Peace Script AI ภายใน 5 นาที**

---

## 🚀 การเริ่มต้นด่วน

### สำหรับผู้ใช้ทั่วไป (ไม่ต้องติดตั้ง)

1. **เข้าสู่เว็บไซต์**
   ```
   https://peace-script-ai.web.app
   ```

2. **สร้างบัญชี**
   - คลิก "สมัครสมาชิก"
   - กรอกอีเมล และรหัสผ่าน
   - ยืนยันอีเมล

3. **เริ่มสร้างสคริปต์**
   - คลิก "สร้างสคริปต์ใหม่"
   - ตอบคำถาม 5 ข้อ
   - รับสคริปต์ทันที!

---

## 💻 การติดตั้งสำหรับนักพัฒนา

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
# Clone repository
git clone https://github.com/metapeaceDev/Peace-Scrip-Ai.git
cd Peace-Scrip-Ai

# ติดตั้ง packages
npm install
```

### ขั้นตอนที่ 2: ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local`:

```bash
# Firebase
VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Google Gemini
VITE_GEMINI_API_KEY=your_gemini_api_key
```

### ขั้นตอนที่ 3: รันแอปพลิเคชัน

```bash
# Development mode
npm run dev

# เปิดเบราว์เซอร์ไปที่
http://localhost:5173
```

---

## 📝 การใช้งานขั้นพื้นฐาน

### 1. สร้างสคริปต์ครั้งแรก

```
หน้าแรก → "สร้างสคริปต์ใหม่" → ตอบคำถาม 5 ข้อ
```

**คำถามที่ 1:** เรื่องอะไร?
- ตัวอย่าง: "การเอาชนะความกลัว"

**คำถาม ที่ 2:** แนว/สไตล์?
- ตัวอย่าง: "ดราม่าสร้างแรงบันดาลใจ"

**คำถามที่ 3:** ความยาว?
- สั้น (1-2 นาที)
- กลาง (2-3 นาที)  ✓
- ยาว (3-5 นาที)

**คำถามที่ 4:** แก่นธรรม?
- เลือกจาก 37 หัวข้อ
- ตัวอย่าง: "กตัญญูกตเวที"

**คำถามที่ 5:** ตัวละคร?
- จำนวน: 1-4 ตัว
- อายุ: เด็ก/วัยรุ่น/ผู้ใหญ่/สูงอายุ
- บุคลิก: ร่าเริง/ใจเย็น/กระตือรือร้น

### 2. แก้ไขสคริปต์

```typescript
// แก้ไขชื่อตัวละคร
script.characters[0].name = "สมชาย";

// แก้ไขบทพูด
script.scenes[0].dialogues[0].text = "วันนี้เราจะเรียนรู้เรื่อง...";

// เพิ่มฉาก
script.scenes.push({
  sceneNumber: 3,
  location: "วัด",
  time: "เช้า",
  dialogues: [...]
});
```

### 3. Export สคริปต์

```
แท็บ Script → "Export" → เลือกรูปแบบ
```

รูปแบบที่รองรับ:
- ✓ PDF (พร้อมรูปแบบมืออาชีพ)
- ✓ TXT (ข้อความธรรมดา)
- ✓ JSON (สำหรับนักพัฒนา)

---

## 🎬 การสร้างวิดีโอ (ขั้นสูง)

### เปิดใช้งาน Video Generation

**ตัวเลือก 1: ใช้ Gemini API (ง่ายที่สุด)**
```bash
# เพียงแค่มี Gemini API key
VITE_GEMINI_API_KEY=your_key
```

**ตัวเลือก 2: ใช้ RunPod Cloud GPU**
```bash
# สำหรับคุณภาพสูงสุด
VITE_RUNPOD_API_KEY=your_runpod_key
VITE_RUNPOD_POD_ID=your_pod_id
```

**ตัวเลือก 3: ใช้ ComfyUI ท้องถิ่น**
```bash
# ติดตั้ง ComfyUI
git clone https://github.com/comfyanonymous/ComfyUI
cd ComfyUI
pip install -r requirements.txt

# รัน ComfyUI
python main.py --listen 0.0.0.0 --port 8188

# ตั้งค่า environment
VITE_COMFYUI_LOCAL_URL=http://localhost:8188
```

### สร้างวิดีโอ

```
แท็บ Storyboard → เลือกฉาก → "Generate Video"
```

ระบบจะ:
1. สร้างภาพตัวละคร (Character Consistency)
2. สร้างภาพฉาก (Scene Generation)
3. เพิ่มการเคลื่อนไหว (AnimateDiff)
4. เพิ่มเสียง (Text-to-Speech)

---

## 🧘 คุณสมบัติพิเศษ

### Buddhist Psychology Integration

```typescript
// ใช้งาน Buddhist Psychology ในสคริปต์
const script = await scriptGenerator.generate({
  theme: "เอาชนะความกลัว",
  buddhistPrinciples: [
    "กตัญญูกตเวที",  // ความกตัญญูกตเวที
    "อินทรียสังวร"    // การควบคุมอินทรีย์
  ],
  psychologyDepth: "advanced" // basic | intermediate | advanced
});
```

ระบบจะ:
- ✓ วิเคราะห์ธรรมะที่เหมาะสม
- ✓ สอดแทรกหลักจิตวิทยาพุทธ
- ✓ สร้างเนื้อหาที่เข้าใจง่าย
- ✓ มีคุณค่าทางจิตใจ

### Multi-Language Support

```typescript
// สลับภาษา
i18n.changeLanguage('en'); // English
i18n.changeLanguage('th'); // ไทย
```

### Team Collaboration

```
โปรเจ็กต์ → "แชร์กับทีม" → เพิ่มสมาชิก
```

สิทธิ์:
- **Owner:** ควบคุมเต็มรูปแบบ
- **Editor:** แก้ไขได้
- **Viewer:** ดูอย่างเดียว

---

## 🛠️ การแก้ไขปัญหา

### ปัญหา: "Cannot connect to backend"

```bash
# ตรวจสอบ environment variables
echo $VITE_GEMINI_API_KEY

# ทดสอบ API
curl https://generativelanguage.googleapis.com/v1beta/models \
  -H "x-goog-api-key: $VITE_GEMINI_API_KEY"
```

### ปัญหา: "Video generation failed"

```bash
# ตรวจสอบ ComfyUI
curl http://localhost:8188/system_stats

# ตรวจสอบ GPU memory
nvidia-smi
```

### ปัญหา: "Test failures"

```bash
# รัน tests
npm test

# Clear cache
rm -rf node_modules
npm install
```

---

## 📊 ตัวอย่างการใช้งาน

### Case 1: สร้างสคริปต์ธรรมะสำหรับเด็ก

```typescript
const script = await scriptGenerator.generate({
  topic: "ความกตัญญู",
  style: "การ์ตูนสนุกสนาน",
  length: "short",
  targetAge: "children",
  buddhistPrinciple: "กตัญญูกตเวที",
  characters: [
    { name: "น้องกระต่าย", age: "child", personality: "ร่าเริง" },
    { name: "คุณแม่", age: "adult", personality: "อบอุ่น" }
  ]
});

// Export เป็น PDF
await scriptExporter.exportPDF(script);
```

### Case 2: สร้างสคริปต์สำหรับผู้ใหญ่

```typescript
const script = await scriptGenerator.generate({
  topic: "การจัดการความเครียด",
  style: "ดราม่าสร้างแรงบันดาลใจ",
  length: "medium",
  targetAge: "adult",
  buddhistPrinciple: "สติ-สัมปชัญญะ",
  psychologyDepth: "advanced",
  characters: [
    { name: "พี่โจ", age: "adult", personality: "เครียด" },
    { name: "อาจารย์", age: "elderly", personality: "ใจเย็น" }
  ]
});
```

### Case 3: สร้างวิดีโออัตโนมัติ

```typescript
// สร้างสคริปต์
const script = await scriptGenerator.generate({...});

// สร้างวิดีโอทุกฉาก
for (const scene of script.scenes) {
  const video = await videoGenerator.generate({
    scene: scene,
    characters: script.characters,
    style: "anime",
    fps: 24,
    duration: scene.estimatedDuration
  });
  
  console.log(`Scene ${scene.sceneNumber}: ${video.url}`);
}
```

---

## 📈 Tips & Best Practices

### การเขียนสคริปต์ที่ดี

1. **กำหนดเป้าหมายชัดเจน**
   - อายุกลุ่มเป้าหมาย
   - ข้อความที่ต้องการสื่อ
   - อารมณ์ที่ต้องการสร้าง

2. **เลือกตัวละครที่เหมาะสม**
   - ส่วนมาก 2-3 ตัวเพียงพอ
   - บุคลิกต้องตรงข้ามเพื่อสร้าง dynamic
   - อายุต้องเหมาะกับเรื่อง

3. **สอดแทรกธรรมะอย่างเป็นธรรมชาติ**
   - ไม่แยกธรรมะออกจากเรื่อง
   - ใช้สถานการณ์จริงในชีวิต
   - สร้างความเข้าใจง่าย

### การสร้างวิดีโอที่ดี

1. **เลือก backend ที่เหมาะสม**
   - Gemini: ง่าย เร็ว ถูก
   - RunPod: คุณภาพสูง ควบคุมได้มาก
   - Local ComfyUI: ฟรี ความเป็นส่วนตัว

2. **ออกแบบ character ให้ชัดเจน**
   - ใช้ consistent style
   - สร้าง reference image
   - ใช้ IP-Adapter สำหรับ consistency

3. **จัดการ cost**
   - ใช้ local สำหรับ testing
   - ใช้ cloud สำหรับ production
   - Monitor usage real-time

---

## 🎯 Roadmap

### Phase 3 (กำลังพัฒนา)
- [ ] AnimateDiff V3 integration
- [ ] IP-Adapter V2 character consistency
- [ ] LoRA fine-tuning support
- [ ] Advanced camera controls
- [ ] Multi-character scenes

### Future Features
- [ ] Mobile app (iOS/Android)
- [ ] Voice cloning
- [ ] Real-time collaboration
- [ ] API for third-party apps
- [ ] Marketplace for templates

---

## 📚 เอกสารเพิ่มเติม

- [คู่มือการติดตั้งแบบละเอียด](./INSTALLATION_GUIDE.md)
- [คู่มือการ Deploy](./DEPLOYMENT_GUIDE.md)
- [RunPod Setup Guide](./docs/RUNPOD_SETUP.md)
- [API Documentation](./docs/API.md)
- [Contributing Guidelines](./CONTRIBUTING.md)

---

## 🆘 ขอความช่วยเหลือ

### Community
- GitHub Issues: [Report Bug](https://github.com/metapeaceDev/Peace-Scrip-Ai/issues)
- Discussions: [Ask Questions](https://github.com/metapeaceDev/Peace-Scrip-Ai/discussions)

### Documentation
- [FAQ](./docs/FAQ.md)
- [Troubleshooting](./docs/TROUBLESHOOTING.md)
- [Video Tutorials](./docs/TUTORIALS.md)

---

**ขอให้สนุกกับการสร้างสรรค์! 🎬✨**

**Last Updated:** December 18, 2024  
**Version:** 1.0
