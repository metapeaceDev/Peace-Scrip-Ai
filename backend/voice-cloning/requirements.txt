# Voice Cloning Server Dependencies

# Core Framework
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Coqui TTS - Voice Cloning Engine
TTS>=0.22.0
transformers==4.33.0  # Pin to version compatible with TTS 0.22.0 (BeamSearchScorer available)

# Audio Processing - Pin to PyTorch <2.6 to avoid weights_only=True issues with TTS
torch>=2.1.0,<2.6.0
torchaudio>=2.1.0,<2.6.0
librosa>=0.10.0
soundfile>=0.12.1
pydub>=0.25.1

# Utilities
python-dotenv==1.0.0
werkzeug==3.0.0
