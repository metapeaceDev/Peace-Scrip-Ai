# .env.production Template
# คัดลอกไฟล์นี้เป็น .env.production และกรอก API keys จริงของคุณ

# =============================================================================
# 🔥 Firebase Configuration (จาก Firebase Console)
# =============================================================================
# ไปที่: https://console.firebase.google.com/
# Project Settings > General > Your apps > Config
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=peace-script-ai.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=peace-script-ai
VITE_FIREBASE_STORAGE_BUCKET=peace-script-ai.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=

# =============================================================================
# 🤖 Gemini API (REQUIRED - Fallback & Quick Generation)
# =============================================================================
# ไปที่: https://aistudio.google.com/app/apikey
# ✅ Phase 1: Primary (ฟรี 1,500 รูป/วัน)
# ✅ Phase 2: Fallback + Quick gen (เมื่อใช้ ComfyUI)
VITE_GEMINI_API_KEY=

# =============================================================================
# 🎯 Image Generation Strategy
# =============================================================================
VITE_PREFERRED_IMAGE_MODEL=auto
# Options:
#   auto          → Smart cascade (ComfyUI → Gemini Pro → Gemini Flash → Pollinations)
#   comfyui-sdxl  → Force ComfyUI SDXL (custom LoRA, Face ID)
#   comfyui-flux  → Force ComfyUI FLUX (highest quality)
#   gemini-pro    → Force Gemini Pro (Imagen 3)
#   gemini-flash  → Force Gemini Flash (fast & free)
#   pollinations  → Free Stable Diffusion

# =============================================================================
# 🚀 Phase 2: Hybrid System (ComfyUI + Gemini)
# =============================================================================

# Enable ComfyUI for advanced features
VITE_COMFYUI_ENABLED=false
# Set to true when:
#   - Need custom LoRA models
#   - Need Face ID (InstantID)
#   - Have 500+ active users
#   - Budget > ฿3,000/month

# Backend Service URL (Render.com)
VITE_USE_COMFYUI_BACKEND=false
VITE_COMFYUI_SERVICE_URL=
# Production: https://peace-script-backend.onrender.com
# Local dev: http://localhost:8000

# ⚙️ Hybrid Configuration
# When VITE_COMFYUI_ENABLED=true:
#   1. Try ComfyUI first (custom features)
#   2. Fallback to Gemini (if ComfyUI busy/down)
#   3. Fallback to Pollinations (last resort)
# 
# When VITE_COMFYUI_ENABLED=false:
#   1. Use Gemini directly (Phase 1)
#   2. Fallback to Pollinations

# Feature Flags (Phase 2)
VITE_ENABLE_FACE_ID=false
VITE_ENABLE_CUSTOM_LORA=false
VITE_ENABLE_ADVANCED_FEATURES=false

# =============================================================================
# 💳 Stripe Payment (Optional - สำหรับ Subscription)
# =============================================================================
# ไปที่: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLISHABLE_KEY=
VITE_STRIPE_SECRET_KEY=

# =============================================================================
# 🔊 Azure Speech Service (ถ้าใช้ TTS)
# =============================================================================
VITE_AZURE_SPEECH_KEY=
VITE_AZURE_SPEECH_REGION=

# =============================================================================
# 🔐 Sentry Error Tracking (Optional)
# =============================================================================
VITE_SENTRY_DSN=

# =============================================================================
# 📊 Analytics (Optional)
# =============================================================================
VITE_GA_MEASUREMENT_ID=
