# 🚀 Quick Start: Admin Dashboard

**เวลาที่ใช้:** 5-10 นาที  
**ความยาก:** ⭐⭐☆☆☆ (ง่าย)

---

## ขั้นตอนที่ 1: ดาวน์โหลด Service Account Key (2 นาที)

1. เปิด [Firebase Console](https://console.firebase.google.com/)
2. เลือกโปรเจค **peace-script-ai**
3. ไปที่ **⚙️ Project Settings** (ด้านข้าย)
4. เลือกแท็บ **Service Accounts**
5. คลิก **Generate New Private Key**
6. บันทึกไฟล์เป็น `service-account-key.json`
7. วางไฟล์ใน folder โปรเจค:
   ```
   peace-script-basic-v1/
   └── service-account-key.json  ← ตรงนี้
   ```

⚠️ **สำคัญ:** อย่าแชร์ไฟล์นี้กับใคร!

---

## ขั้นตอนที่ 2: ติดตั้ง Firebase Admin (1 นาที)

เปิด Terminal ในโฟลเดอร์โปรเจค:

```bash
cd /Users/surasak.peace/Desktop/peace-script-basic-v1
npm install firebase-admin
```

รอจนเสร็จ (ประมาณ 30 วินาที)

---

## ขั้นตอนที่ 3: หา User ID ของคุณ (1 นาที)

### วิธีที่ 1: ใช้ Firebase Console (ง่ายที่สุด)

1. เปิด [Firebase Console](https://console.firebase.google.com/)
2. ไปที่ **Authentication** → **Users**
3. หา email ของคุณ
4. คัดลอก **User UID** (เช่น `abc123xyz456...`)

### วิธีที่ 2: ใช้ Browser Console

1. เปิด https://peace-script-ai.web.app
2. Login เข้าระบบ
3. กด F12 เปิด Developer Tools
4. ไปแท็บ **Console**
5. พิมพ์:
   ```javascript
   auth.currentUser.uid
   ```
6. กด Enter → จะได้ User ID

---

## ขั้นตอนที่ 4: Grant Admin Access (1 นาที)

ใน Terminal:

```bash
# แทนที่ YOUR_USER_ID ด้วย UID ที่คุณได้จากขั้นตอนที่ 3
node scripts/set-admin-claims.js YOUR_USER_ID super-admin
```

**ตัวอย่าง:**
```bash
node scripts/set-admin-claims.js abc123xyz456def789 super-admin
```

**ผลลัพธ์ที่ต้องการ:**
```
✓ Successfully set super-admin role for user: abc123xyz456def789
✓ Custom claims updated
✓ User can now access admin dashboard
```

---

## ขั้นตอนที่ 5: ทดสอบ (2 นาที)

1. **Logout** จากเว็บไซต์
2. **Login** เข้าใหม่
3. ดูที่มุมขวาบน → ควรเห็นปุ่ม **Admin** (สีแดง-ส้ม)
4. คลิกปุ่ม Admin
5. สำรวจ 3 แท็บ:
   - 📊 **Analytics & Users** - ดูข้อมูลและกราฟ
   - 👥 **Admin Management** - ดูรายชื่อ admin
   - 🔔 **Alerts** - ดูการแจ้งเตือน

---

## 🎉 เสร็จแล้ว!

ตอนนี้คุณสามารถ:
- ✅ ดูสถิติผู้ใช้ทั้งหมด
- ✅ ดูรายได้ (MRR/ARR)
- ✅ ดูการใช้งาน (Credits, API, Veo)
- ✅ ค้นหาและกรองผู้ใช้
- ✅ ดูรายละเอียดผู้ใช้
- ✅ Export ข้อมูลเป็น CSV
- ✅ จัดการ admin users
- ✅ ดู alerts

---

## 🔧 คำสั่งที่มีประโยชน์

### ดู admin ทั้งหมด:
```bash
node scripts/set-admin-claims.js list
```

### Grant admin ระดับอื่น:
```bash
# Admin (ไม่สามารถจัดการ admin users)
node scripts/set-admin-claims.js USER_ID admin

# Viewer (ดูได้อย่างเดียว)
node scripts/set-admin-claims.js USER_ID viewer
```

### ถอนสิทธิ์ admin:
```bash
node scripts/set-admin-claims.js USER_ID revoke
```

### ดู permissions ของ admin:
```bash
node scripts/set-admin-claims.js USER_ID permissions
```

---

## ❓ แก้ปัญหา

### ไม่เห็นปุ่ม Admin
1. ลอง Logout/Login อีกครั้ง
2. เช็คว่ารันคำสั่ง grant admin สำเร็จหรือไม่
3. ลองรีเฟรชหน้าเว็บ (Ctrl+F5 หรือ Cmd+Shift+R)

### Error: Cannot find module 'firebase-admin'
```bash
npm install firebase-admin
```

### Error: service-account-key.json not found
- ตรวจสอบว่าไฟล์อยู่ใน folder โปรเจคจริงๆ
- ชื่อไฟล์ต้องตรง: `service-account-key.json`

### ปุ่ม Admin เห็นแต่กดแล้ว Access Denied
- User นั้นอาจไม่มี admin claim จริง
- ลองรันคำสั่ง grant admin อีกครั้ง
- ตรวจสอบ Firestore Rules ว่า deploy แล้ว

---

## 📚 เอกสารเพิ่มเติม

- **คู่มือ Setup แบบละเอียด:** `ADMIN_SETUP_GUIDE.md`
- **รายงานการพัฒนา:** `ADMIN_DASHBOARD_COMPLETE.md`
- **User Management:** `ADMIN_USER_MANAGEMENT_SUCCESS.md`

---

## 🆘 ต้องการความช่วยเหลือ?

1. อ่าน `ADMIN_SETUP_GUIDE.md` (คู่มือละเอียด)
2. ตรวจสอบ Firestore Rules ที่ Firebase Console
3. ดู Console log ใน Browser (F12)

---

**Status:** ✅ Admin Dashboard พร้อมใช้งาน  
**URL:** https://peace-script-ai.web.app  
**Version:** 1.0.0
